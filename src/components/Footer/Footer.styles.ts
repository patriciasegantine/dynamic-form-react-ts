import styled from 'styled-components';

export const FooterContainer = styled.footer`
  padding: 1rem;

  position: absolute;
  bottom: 0;
  right: 0;

  p {
    font-size: .6rem;
    font-style: italic;
    }
`